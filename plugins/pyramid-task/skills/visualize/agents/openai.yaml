interface:
  display_name: "Visualize Pyramid Plan"
  short_description: "Explore a focused live task execution graph"
  default_prompt: "Use $visualize to open a focused live graph that updates only after validated semantic changes."
