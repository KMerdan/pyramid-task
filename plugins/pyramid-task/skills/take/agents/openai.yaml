interface:
  display_name: "Take Pyramid Task"
  short_description: "Claim one task with its scoped packet"
  default_prompt: "Use $take to inspect the compact frontier, claim one ready task with its task-scoped guard, and return only its task packet."
