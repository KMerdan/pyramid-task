interface:
  display_name: "Inspect Pyramid Plan"
  short_description: "Query exact task context without graph overload"
  default_prompt: "Use $inspect to answer this question with the smallest exact runtime query, expanding only the relevant node or assurance detail."
