interface:
  display_name: "Inspect Pyramid Plan"
  short_description: "Query exact status and safe parallel groups"
  default_prompt: "Use $inspect to answer with the smallest exact runtime query, including the derived parallel-ready frontier only when concurrent execution is relevant."
